const initialState = [
  {
    company: 'Apple Computer',
    symbol: 'AAPL',
    purchasePrice: 123
  }

]

export default function positions(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
